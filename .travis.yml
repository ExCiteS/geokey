language: python

python:
  - "2.7"

services:
  - postgresql

addons:
  postgresql: "9.4"

before_script:
  - psql template1 postgres -c 'create extension hstore;'
  - psql -c 'create database geokey;' -U postgres
  - psql -U postgres -d geokey -c "create extension postgis;"
  - cp geokey/core/settings/project.sample.py geokey/core/settings/project.py
  - cp geokey/core/url/extensions.sample.py geokey/core/url/extensions.py
  - pip install --process-dependency-links -e .
  - python geokey/manage.py migrate

install:
  - pip install coveralls

script:
  - coverage run --source=applications,projects,categories,contributions,datagroupings,users,superusertools,extensions ./runtests.py

after_success: coveralls
