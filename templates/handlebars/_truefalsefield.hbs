<h5>{{name}} ({{key}})</h5>
<div class="form-group">
    <label for="{{key}}-ref" class="col-sm-3 col-sm-offset-1 control-label">is</label>
    <div class="col-sm-7">
        <select name="{{key}}-ref" id="reference-value" class="form-control">
            <option></option>
            <option value="true">True</option>
            <option value="false">False</option>
        </select>
    </div>
</div>